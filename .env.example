# Required. Hygraph Content API endpoint (Project Settings > Access).
# Public Content API permissions can be enough for PUBLISHED content.
HYGRAPH_ENDPOINT=

# Optional. Permanent Auth Token, used only on the server.
# Needed if public Content API permissions are disabled.
# Never use NEXT_PUBLIC_ for this value.
HYGRAPH_TOKEN=
